plugins {
    id 'war'
}

version '1.0.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    providedCompile (group: 'javax.servlet', name: 'javax.servlet-api', version: '3.1.0')
}
